# 私有预览验收记录

日期：2026-09-21。目标：Discourse `5b59681a8c85d1b8cca5c8eac15debc389c53325`。当前为私有预览，旧树洞生产入口未切换。

## 后端

隔离数据库回归：18 个测试、159 项断言全部通过，覆盖：

- 校友访问、管理员委派、管理员预览、只读、被限制账号及越权写入。
- 匿名 JSON 不含真实用户名/用户 ID、帖内别名稳定、隐藏父回复不泄露正文。
- 可见性、鉴权图片、上传归属、九图上限、论坛用户删除/匿名化/合并的隐私清理。
- 最新/最近回复排序、列表分页、楼层链接定位、我的回复。
- 互斥赞踩、聚合通知、举报去重及处理后重开、身份审计与撤权后幂等缓存失效。
- 字数限制、跨帖父回复拒绝、重复请求、历史导入、缺图显式确认。
- 论坛铃铛不带真实操作者，已读操作只影响本人的树洞通知。

## 数据演练

真实快照在独立数据库完成逐字段核对：帖子、回复关系、正文、时间、状态、旧版匿名别名、赞踩、举报、审计、消息及已读状态、媒体完整性。旧版实际匿名命名函数作为独立对照，所有可见回复别名一致。

导入不生成历史通知投递事件。无业务引用的旧访客账号只保留档案，没有补造论坛身份；业务实际引用的账号均验证存在。当前用户组来自论坛，旧权限缓存不作为授权依据。

源站已有一张图片文件缺失，明确记录并显示缺图提示；其他引用图片完成哈希校验和保护性转换。一个旧回复计数与实际可见回复数不符，新版采用实际数量，原始数值仍保存在私有档案。

原始快照、账号对应关系、图片、完整对账报告及操作日志不在此公有仓库中。

## 浏览器

Chromium 自动流程通过：连续加载、浏览器返回恢复列表、发布和上传、引用回复、身份隔离、未登录图片拒绝、赞和铃铛、举报处理、公开身份查看审计、自己的帖子/回复/数据导出、消息已读、搜索、共同侧栏和跨应用导航。

实际检查深色/浅色主题，320 / 390 / 768 / 1440 像素宽度下列表及楼层无横向溢出。页面无 JavaScript 异常。真实快照验证只读禁止写入，旧路径转换正常。

## 构建和导出

前端插件及样式资源编译通过。只读导出工具用合成文件验证默认缺图失败、显式缺图清单、媒体校验和/复制、输出权限及路径越界拒绝。

本轮未进行生产切换、大规模并发压力测试或邮件/浏览器推送测试；后两种通知并未提供。上线需冻结旧写入后重新导出最终数据，不能使用已过时的测试快照。
